# rust
target/

# node
node_modules/

# build output（alfred/dist 是分发包，需入库；raycast/dist 是编译产物，不入库）
/server/web/dist/
raycast/dist/

# runtime data (sqlite db, generated token)
data/
*.db
*.db-wal
*.db-shm

# os / editor
.DS_Store

# AI 协作工作区（本地私有，不入库）
.pi/
.trellis/
.agents/
